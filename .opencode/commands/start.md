# start

Start the CyberPDV server:

1. Run `npm start` in the project root
2. Wait for "Server running on port 3000"
3. Confirm with `curl http://localhost:3000`
4. Return the server URL to the user
